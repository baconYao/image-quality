[project]
name = "psnr-toolkit"
version = "0.1.0"
description = "OpenCV-only (no ffmpeg) PSNR/SSIM/MS-SSIM visual-degradation comparison toolkit, managed with uv."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
    "opencv-contrib-python>=5.0.0.93",
]

[tool.uv]
package = false
